spring:
  main:
    allow-bean-definition-overriding: true
jwt:
  secret: secret
mongock:
  enabled: true
  migration-scan-package: com.example.blogservice

init:
  users:
    location: some_wrong_file.json