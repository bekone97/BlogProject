mongock:
    enabled: false
    migration-scan-package: com.example.blogservice.changeunit
server:
    port: 8080

jwt:
    secret: secret
spring:
  profiles:
        active: test
init:
  users:
    location: some_wrong_file.json